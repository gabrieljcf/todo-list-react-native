import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    borderTopColor: "#333",
    borderTopWidth: 1,
  },
  image: {
    marginTop: 48,
    marginBottom: 16,
  },
  textBold: {
    color: "#808080",
    fontSize: 14,
    fontWeight: "bold",
  },
  text: {
    color: "#808080",
    fontSize: 14,
  },
});
